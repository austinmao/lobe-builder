id: TASK-001
title: "Fix category filter E2E test selector mismatch"
objective: |
  Update E2E test step definitions to use the correct selector for category filtering.
  The tests currently use `[data-testid="category-filter"]` but the actual component 
  uses `[data-testid="category-menu"]` (see src/app/[variants]/(main)/discover/components/CategoryMenu.tsx:37).
  
  This fix addresses 2 failing tests:
  - DISCOVER-INTERACT-002: Filter assistants by category
  - DISCOVER-INTERACT-008: Filter MCP tools by category
scope:
  - "Update selector in `I click on a category in the category filter` step definition"
  - "Ensure test waits for category-menu to be visible"
  - "Verify both assistant and MCP category filtering works"
constraints:
  - "Must not modify production UI components"
  - "Must maintain backward compatibility with existing passing tests"
  - "Time constraint: 15-30 minutes"
acceptance_criteria:
  - "BLOCKING: DISCOVER-INTERACT-002 test passes when run with `pnpm --filter @lobechat/e2e-tests test`"
  - "BLOCKING: DISCOVER-INTERACT-008 test passes when run with `pnpm --filter @lobechat/e2e-tests test`"
  - "All previously passing smoke tests continue to pass"
deliverables:
  - "e2e/src/steps/discover/interactions.steps.ts - updated selector on lines 52-54"
risks:
  - "The category-menu selector may be too generic if multiple menus exist on page - use more specific parent selectors if needed"
out_of_scope:
  - "Adding new data-testid attributes to production code"
  - "Modifying other test step definitions"
estimated_minutes: 20
dependencies: []
phase: "testing"
status: pending
created_date: "2025-12-10"
testing_requirements:
  coverage: "N/A - test infrastructure fix"
  required_tests:
    - "Run DISCOVER-INTERACT-002 and verify pass"
    - "Run DISCOVER-INTERACT-008 and verify pass"
    - "Run smoke suite to verify no regressions"
  mocking_policy:
    allowed: []
    forbidden: []
  validation_commands:
    - "pnpm --filter @lobechat/e2e-tests test -- --tags '@interactions'"
    - "pnpm --filter @lobechat/e2e-tests test:smoke"
