id: TASK-014
title: "Add Hero section with content"
objective: |
  Populate the Hero, Features, and CTA sections with actual content for the
  "Softening the Season" landing page.
scope:
  - Add Hero section with title, subtitle, CTA button
  - Add Features section with 3 feature items
  - Add CTA section with registration prompt
  - Use content from PRD specification
constraints:
  - Follow exact section structure from PRD
  - Use icons that exist in design system
  - Time: 30 minutes max
acceptance_criteria:
  - "BLOCKING: Hero section renders with correct title"
  - "BLOCKING: Features section shows 3 items"
  - "BLOCKING: CTA section has registration button"
  - "All text matches PRD specification"
deliverables:
  - Complete page content in Payload
  - Screenshot of rendered page
risks:
  - Icon names may not match design system
  - Section structure may differ
out_of_scope:
  - Images/media assets
  - Form functionality
  - Analytics tracking
estimated_minutes: 30
dependencies: ["TASK-013"]
phase: "phase3-landing-page"
status: pending
created_date: "2025-12-10"
testing_requirements:
  coverage: "100% - content validation"
  required_tests:
    - "Hero title renders"
    - "Features items render (count = 3)"
    - "CTA button renders"
    - "Content matches specification"
  validation_commands:
    - "cd lobe-builder && npx playwright test tests/e2e/ceremonia/phase3-landing-page.spec.ts --grep 'render complete content'"
