id: TASK-003
title: "Fix pagination URL assertion in E2E test"
objective: |
  Update the E2E test assertion for pagination to handle the case where page 1 might not 
  include `page=1` in the URL (common pattern to omit page=1 for cleaner URLs).
  
  Current test at e2e/src/steps/discover/interactions.steps.ts:316-322 expects URL to contain
  `page=` or `p=` parameter, but the application may use a different pattern or omit page=1.
  
  This fix addresses:
  - DISCOVER-INTERACT-003: Navigate to next page of assistants
scope:
  - "Analyze actual URL pattern used by pagination component"
  - "Update test assertion to match actual behavior"
  - "Handle edge case where page=1 is omitted from URL"
constraints:
  - "Must understand actual pagination URL behavior first"
  - "Should not change pagination component unless absolutely necessary"
  - "Time constraint: 20-30 minutes"
acceptance_criteria:
  - "BLOCKING: DISCOVER-INTERACT-003 test passes when clicking next page"
  - "Test correctly validates that pagination changes page"
  - "All smoke tests continue to pass"
deliverables:
  - "e2e/src/steps/discover/interactions.steps.ts - updated assertion at lines 316-322"
risks:
  - "Pagination may use URL hash or other mechanism - need to verify actual behavior"
  - "May need to test both page=2 and page=1 scenarios"
out_of_scope:
  - "Modifying pagination component logic"
  - "Adding new pagination tests"
estimated_minutes: 25
dependencies: []
phase: "testing"
status: pending
created_date: "2025-12-10"
testing_requirements:
  coverage: "N/A - test infrastructure fix"
  required_tests:
    - "Run DISCOVER-INTERACT-003 and verify pass"
    - "Manually verify pagination URL changes in browser"
  validation_commands:
    - "pnpm --filter @lobechat/e2e-tests test -- --name 'Navigate to next page'"
